const $ = require( "jquery" );

// const searchInput = require('../scripts/components/search-input');
// const navbar = require('../scripts/components/navbar');
const root = require('../scripts/root');

$(document).ready(() => {


    root.root.function.init();
    root.root.event();

    // searchInput.function.init();
    // searchInput.event();

    // navbar.function.init();
    // navbar.event();

});